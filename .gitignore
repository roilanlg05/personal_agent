vendor/

.DS_Store
*.png
.claude/
images/
# MLX spike (venv + downloaded weights, not for git) — but keep the launcher script
spike-mlx/*
!spike-mlx/serve_mlx_vlm.py

# llama.cpp spike (full fork clone — investigated and decided against; not for git)
spike-llamacpp/

# graphify
# Cache (AST + stat-index) is regenerable from source — never check it in.
# Keep the indexed outputs: GRAPH_REPORT.md, graph.json, manifest.json, .graphify_labels.json, dated snapshots.
graphify-out/cache/

# gemma-memory/ lives in its own repo now (github.com/roilanlg05/gemma-memory).
# Ignore the folder entirely so a local re-clone for dev convenience doesn't pollute git status here.
gemma-memory/

# Stray root-level (residual from before the gemma-memory/ split)
/.env
/docker-data/
